# News

## 0.1.0 — initial release

- First public release of the code accompanying the ChroCompare manuscript
  (`DRAFT18_paper.pdf`): fingerprint generation, model/calibration training,
  test-set evaluation, single-pair prediction (`run_single.py`), and result
  analysis/plotting scripts.
